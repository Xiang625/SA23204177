% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Wang2020.R
\name{Wang2020}
\alias{Wang2020}
\title{Testing of mean vector using R}
\usage{
Wang2020(sam, m, n)
}
\arguments{
\item{sam}{the matrix of samples}

\item{m}{trimming parameter}

\item{n}{sample size}
}
\value{
\code{TRUE} (reject null hypothesis) or \code{FALSE}
}
\description{
Testing of mean vector using R
}
\examples{
\dontrun{
library(MASS)
set.seed(1234)
n1 <- n2 <- 50
p <- 200
mu1 <- rep(0, p)
mu2 <- mu1
mu2[1:10] <- 1
true.cov <- 0.4^(abs(outer(1:p, 1:p, "-"))) # AR1 covariance
sam1 <- mvrnorm(n = n1, mu = mu1, Sigma = true.cov)
sam2 <- mvrnorm(n = n2, mu = mu2, Sigma = true.cov)
Wang2020(sam1, m=5, n=n1)
Wang2020(sam2, m=5, n=n2)
}
}
